/**
 * Created with JetBrains WebStorm.
 * User: allanma
 * Date: 12-5-10
 * Time: 下午3:13
 */

(function ($, undefined) {
    /*
     * Controller
     * 负责
     * 可持有Model和View对象
     * 监听Model对象事件，对Model对象发各种请求，获取更新数据
     * 监听View对象的事件，通知View Render，Update等
     * 管理URL Router等逻辑模块
     * */

    var Controller = $.lang.Class.inherit({

    });

})(Mallan);
